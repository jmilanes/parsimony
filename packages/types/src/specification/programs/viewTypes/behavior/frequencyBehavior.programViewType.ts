import { ProgramViewTypes } from "../../program.enums";
import { BaseBehaviorView } from "./baseBehaviorView";

export class FrequencyBehaviorProgramViewType extends BaseBehaviorView {
  type = ProgramViewTypes.FrequencyBehavior;
}
