import { ProgramViewTypes } from "../../program.enums";

import { BaseBehavior } from "./baseBehaviorView";

export class DurationBehaviorType extends BaseBehavior {
  viewType = ProgramViewTypes.DurationBehavior;
}
