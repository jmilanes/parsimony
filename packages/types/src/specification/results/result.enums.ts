export enum ResultType {
  Duration = "DURATION",
  Frequency = "FREQUENCY",
  Interval = "INTERVAL",
  Trial = "TRIAL"
}

//TODO Merge these
export enum BehaviorType {
  Duration = "DURATION",
  Frequency = "FREQUENCY",
  Interval = "INTERVAL"
}
