overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  ../types/src/types/generated/graphql.types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
