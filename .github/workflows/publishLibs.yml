name: "Publish Libs to NPM 📦"
on:
  workflow_call:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: "18"
          always-auth: true
      - name: Authenticate with private NPM package
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_AUTH_TOKEN }}" > ~/.npmrc
      - name: nmp install
        run: npm install
      - name: Install dependencies and build @parsimony/types 🔧
        run: cd packages/libs/types && npm build
      - name: Back to Root
        run: cd ../../../
      # - name: Install dependencies and build @parsimony/utilities 🔧
      #   run: cd packages/libs/utilities && yarn install && yarn build
      # - name: Back to Root
      #   run: cd ../../../
      # - name: Install dependencies and build @parsimony/bal 🔧
      #   run: cd packages/libs/backend-access && yarn install && yarn build
      # - name: Back to Root
      #   run: cd ../../../
      - name: Workspace Publish
        run: npm workspace foreach --exclude app server cypress utilities bal --no-private yarn publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
