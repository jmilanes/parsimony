name: "Publish Libs to NPM 📦"
on:
  workflow_call:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: "18"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"
      - name: Install dependencies and build @parsimony/types 🔧
        run: cd packages/libs/types && npm install && npm run build
      - name: Publish @parsimony/types on NPM 📦
        run: npm publish
      - name: Install dependencies and build @parsimony/utilities 🔧
        run: cd ../utilities && npm install && npm run build
      - name: Publish @parsimony/utilities on NPM 📦
        run: npm publish
      - name: Install dependencies and build @parsimony/bal 🔧
        run: cd ../backend-access && npm install && npm run build
      - name: Publish @parsimony/bal on NPM 📦
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
